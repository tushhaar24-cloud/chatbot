# Vite only exposes variables prefixed with VITE_ to browser code.
# This is a PUBLIC value - anything here ships in the JS bundle.
# Never put the AI API key or any secret in this file.
VITE_API_BASE_URL=http://localhost:8010
